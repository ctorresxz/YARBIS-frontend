// Rutas para la sección /manualtotal
// Exportamos tanto por nombre como por default para máxima compatibilidad
// con distintos estilos de import en el proyecto.

export const ROUTES_MANUALTOTAL = {
  MANUALTOTAL: "/manualtotal",
};

export type ManualTotalRouteKey = keyof typeof ROUTES_MANUALTOTAL;

export default ROUTES_MANUALTOTAL;
